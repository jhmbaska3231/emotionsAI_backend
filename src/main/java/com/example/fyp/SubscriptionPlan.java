package com.example.fyp;

// example usage to set plan:
// Subscription subscription = new Subscription();
// subscription.setSubscription_plan(SubscriptionPlan.MONTHLY);

// example usage to get plan:
// SubscriptionPlan plan = subscription.getSubscription_plan();
public enum SubscriptionPlan {
    
    MONTHLY,
    YEARLY

}
